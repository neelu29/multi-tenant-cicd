name: Multi-Tenant CI/CD Pipeline (Demo)

on:
  push:
    branches:
      - main
      - user1
      - user2
      - user3

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Demo Docker Build
        run: |
          echo "Pretend building Docker image for branch ${{ github.ref_name }}"
          echo "Image tag would be: multi-tenant-cicd/webapp:${{ github.ref_name }}"

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Demo Kubernetes Deploy
        run: |
          echo "Pretend deploying to Kubernetes namespace ${{ github.ref_name }}"
          echo "Deployment rollout simulated"

      - name: Demo Kafka Notification
        run: |
          echo "Pretend sending Kafka event for branch ${{ github.ref_name }}"

