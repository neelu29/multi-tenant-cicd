apiVersion: v1
kind: Service
metadata:
  name: user1-service
  namespace: user1
spec:
  selector:
    app: user1-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP

