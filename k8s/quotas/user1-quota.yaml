apiVersion: v1
kind: ResourceQuota
metadata:
  name: user1-quota
  namespace: user1
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    pods: "10"

