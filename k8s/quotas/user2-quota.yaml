apiVersion: v1
kind: ResourceQuota
metadata:
  name: user2-quota
  namespace: user2
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    pods: "10"

